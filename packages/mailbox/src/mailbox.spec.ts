import { expect } from 'chai';
// import { Mailbox } from './mailbox';

describe('Mailbox', () => {
  it('should call setup mailbox when creating a user', async () => {
    // const mailbox = await Mailbox.createMailbox(mockUser, mockConfig);
    expect(true).to.equal(true);
  });
});
